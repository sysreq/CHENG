// ============================================================================
// CHENG — Tail Conventional Panel (Stub)
// Track D: Frontend Panels will implement the full version.
// ============================================================================

/**
 * Tail parameters panel for Conventional/T-Tail/Cruciform configurations.
 * Stub implementation — Track D will provide the full version.
 */
export default function TailConventionalPanel() {
  return (
    <div style={{ padding: 12, fontSize: 12, color: 'var(--color-text-muted)' }}>
      Tail conventional panel placeholder (Track D)
    </div>
  );
}
